import styled from 'styled-components';

import { flipInVerRight } from '@/pages/MyHealth/styles';

export const GraphContainer = styled.div`
  animation: ${flipInVerRight} 1s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
`;
